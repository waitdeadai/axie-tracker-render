# Axie tracker — VPS env template. On the VPS: copy to ops/vps/.env.vps and fill REPLACE_ME.
# .env.vps is gitignored; never commit real secrets.
#
# BOOT-REQUIRED (verified via local docker boot 2026-05-26 — app CRASHES at start if any
# of these is missing; env.ts Zod schema is stricter than config.ts):
#   API_KEY, ORIGINS_API_KEYS, LEADERBOARD_URL, BATTLE_LOG_URL, AXIE_TOP_URL
# All are present below with values; just fill the REPLACE_ME secrets.

PORT=4000
NODE_ENV=production
DATA_DIR=/data
PYTHON_BIN=python3

# --- Sky Mavis Origins API (REQUIRED) ---
# Comma-separated keys. These are the paid quota — keep them secret.
ORIGINS_API_KEYS=REPLACE_ME_key1,REPLACE_ME_key2
ORIGINS_BLOG_KEYS=REPLACE_ME_key3,REPLACE_ME_key4,REPLACE_ME_key5
ORIGINS_API_KEY_HEADER=X-API-Key
ORIGINS_BLOG_ENDPOINT=https://api-gateway.skymavis.com/origin/v2
LEADERBOARD_URL=https://api-gateway.skymavis.com/origins/v2/season-leaderboards
BATTLE_LOG_URL=https://api-gateway.skymavis.com/origin/v2/community/users/:client_id/battle-logs
AXIE_TOP_URL=https://axie.top/
AXIE_TOP_SECRET_KEY=REPLACE_ME_axie_top_secret

# --- Payments (Ronin) — wallet + prices are public, safe to keep ---
PAYMENT_WALLET_ADDRESS=ronin:51a8318FBFf6DDFee50Ee0fb0f33AF02F34fF649
RONIN_RPC_URL=https://ronin.drpc.org
USDC_CONTRACT=0x9433e1776c043289c0a5aba2eb9ff8dd1e8471c3
RON_DECIMALS=18
USDC_DECIMALS=6
PLANS_USDC_2WEEKS=2
PLANS_RON_2WEEKS=20
PLANS_USDC_1MONTH=5
PLANS_RON_1MONTH=50
PLANS_USDC_3MONTH=12
PLANS_RON_3MONTH=120
PLANS_USDC_1YEAR=40
PLANS_RON_1YEAR=400
# Backend API key for the paid programmatic layer — generate a random string.
API_KEY=REPLACE_ME_random_secret

# --- Tracker tuning (carried from render.yaml) ---
ACTIVE_WINDOW_MS=300000
REFRESH_LEADERBOARD_MS=3000
BATTLELOG_TTL_MS=45000
IMAGES_TTL_MS=300000
KEYS=5
KEY_RPS=5
KEY_RPM=100
BUDGET_RPM=450
BUDGET_RPS=7.5
GLOBAL_RESERVOIR=450
GLOBAL_REFRESH_MS=60000
REQUEST_TIMEOUT_MS=5000

# --- Access / CORS ---
# Set to the real domain you serve the tracker on. Do NOT use '*'.
CORS_ORIGIN=https://REPLACE_ME.waitdead.com
FRONTEND_URL=https://REPLACE_ME.waitdead.com
ENABLE_SELF_PING=false

# --- Discord auth: INTENTIONALLY DISABLED ---
# Leaving DISCORD_CLIENT_ID/SECRET/CALLBACK unset makes hasDiscordAuthEnabled()
# return false -> the app boots "Discord auth disabled" and clients reach the
# tracker without a Discord login. To re-enable later, set all three plus
# SESSION_SECRET and re-add the client Discord IDs to ALLOWED_DISCORD_IDS.
# DISCORD_CLIENT_ID=
# DISCORD_CLIENT_SECRET=
# DISCORD_CALLBACK_URL=
# SESSION_SECRET=
# JWT_SECRET=
# ALLOWED_DISCORD_IDS=
